/**
 * ------------------------------------------------------------------------------------
 * 保存项目配置信息，使用此文件时需要拷贝到自己项目根目录重命名为 config.gradle，
 * 然后修改一些自己的本地配置信息，此文件不提交到版本库
 *
 * Author: lzan13
 * Time：2017/11/7 12:10
 * ------------------------------------------------------------------------------------
 */
ext {
    /**
     * 公开配置信息
     */
    // 项目引用库等版本配置
    compileSdkVersion = 26
    minSdkVersion = 15
    targetSdkVersion = 22
    versionCode = 1
    versionName = '1.0.0'
}
